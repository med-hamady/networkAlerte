# Core
fastapi[standard]>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0

# Database
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29,<1.0
alembic>=1.14,<2.0

# Configuration
pydantic-settings>=2.7,<3.0

# Scheduler
apscheduler>=3.10,<4.0

# HTTP client
httpx>=0.27,<1.0

# Password hashing (user authentication, login)
passlib[bcrypt]>=1.7,<2.0
bcrypt>=4.0,<5.0

# Network tools
pysnmp-lextudio>=6.1,<7.0
paramiko>=3.4,<4.0
# API RouterOS binaire — repli du blocage client sur le routeur de coeur quand la
# coupure SSH sur le LR n'aboutit pas (cf. mikrotik_service).
librouteros>=3.2,<4.0

# NetFlow collector (top client destinations by operator/CDN)
# - netflow : pure-Python NetFlow v1/v5/v9/IPFIX decoder
# - maxminddb : reads the offline GeoLite2-ASN.mmdb (IP → ASN + org name)
netflow>=0.12,<1.0
maxminddb>=2.6,<4.0

# System metrics
psutil>=5.9,<7.0

# PDF generation (daily saturated-Rockets WhatsApp report)
fpdf2>=2.7,<3.0

# Dev tools
ruff>=0.8,<1.0
pre-commit>=4.0,<5.0

# Testing
pytest>=8.0,<9.0
pytest-asyncio>=0.24,<1.0
